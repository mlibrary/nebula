# Managed by puppet (nebula/profile/kubernetes/haproxy/services.d/https_alt.cfg.erb)
frontend kubernetes-https-alt-front
  bind <%= @public_address %>:8443
  mode tcp
  option tcplog
  default_backend kubernetes-https-alt-back

backend kubernetes-https-alt-back
  mode tcp
  option tcp-check
  balance roundrobin
