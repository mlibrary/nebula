#!/bin/sh
# Managed by puppet (nebula/profile/puppet/query.sh.erb)
exec curl -X POST --tlsv1 \
  https://<%= @puppetdb_server %>:8081/pdb/query/v4/resources \
  --cacert /etc/puppetlabs/puppet/ssl/certs/ca.pem \
  --cert /etc/puppetlabs/puppet/ssl/certs/<%= @trusted['certname'] %>.pem \
  --key /etc/puppetlabs/puppet/ssl/private_keys/<%= @trusted['certname'] %>.pem \
  -H 'Content-Type:application/json' \
  -d "$@"
