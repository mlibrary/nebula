# Managed by puppet (nebula/profile/haproxy/default.erb)

CONFIG="/etc/haproxy/haproxy.cfg"
EXTRAOPTS="<% @balanced_frontends.keys.sort.each do |service| %>-f /etc/haproxy/<%= service %>.cfg <% end %>"

